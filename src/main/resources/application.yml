spring:
  application:
    name: RIS_valanter
  datasource:
    url: jdbc:postgresql://localhost:5432/RIS_valanter
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration
    # БД уже не пустая, но нет flyway_schema_history — иначе Flyway падает при старте
    baseline-on-migrate: true
    # 0 = после baseline всё ещё выполнится V1__init_schema.sql
    baseline-version: 0

# Секрет JWT: в продакшене задавать через переменные окружения
app:
  jwt:
    secret: ${JWT_SECRET:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef}
    expiration-ms: 86400000
  # OpenWeather: ключ по умолчанию для разработки; в проде лучше WEATHER_API_KEY в окружении
  weather:
    api-key: ${WEATHER_API_KEY:9fc3220dd91bcbda7f8adafa7d5464e0}
  # Google Maps: включите Geocoding API в Google Cloud Console, создайте ключ и задайте GOOGLE_MAPS_API_KEY
  google:
    maps-api-key: ${GOOGLE_MAPS_API_KEY:}

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: warn
